name: Linux

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    paths:
      - '.github/workflows/linux.yml'
      - '.github/actions/**'
      - 'src/**'
      - 'test/**'
      - 'CMakeLists.txt'
      - 'cmake/**'

jobs:
  build:
    name: Build ${{ matrix.arch }} ${{ matrix.build_type }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        build_type: [Debug, Release]
        os: [ubuntu-22.04]
        include:
          - os: ubuntu-22.04
            package: QTalk-x86_64
            host: linux
            arch: linux_gcc_64

    # Runs all steps on bash
    defaults:
      run:
        shell: bash

    env:
      QT_VERSION: 6.8.0

    steps:
      - name: Checkout repo
        uses: actions/checkout@v5
        with:
          submodules: recursive
          fetch-depth: 1
          
      - name: Initial setup
        uses: ./.github/actions/common

      - name: Install dependencies
        run: |
          chmod a+x ./tools/setup/install_deps_debian.sh
          sudo ./tools/setup/install_deps_debian.sh

      - name: Install Qt for Linux
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          host: ${{ matrix.host }}
          target: desktop
          arch: ${{ matrix.arch }}
          dir: ${{ runner.temp }}
          modules: qt5compat #qtdeclarative qtquickcontrols2 qtshadertools qtimageformats qt5compat qtmultimedia
          setup-python: false
          cache: true

      - name: Configure
        working-directory: ${{ runner.temp }}/build
        run: ${{ env.QT_ROOT_DIR }}/bin/qt-cmake -S ${{ github.workspace }} -B . -G "Ninja Multi-Config"
              -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
              -DQTK_UNITTEST_BUILD=${{ matrix.build_type == 'Debug' && 'ON' || 'OFF' }}

      - name: Build
        working-directory: ${{ runner.temp }}/build
        run: cmake --build . --target all --config ${{ matrix.build_type }} --parallel

      - name: Verify Executable
        if: matrix.build_type == 'Release'
        working-directory: ${{ runner.temp }}/build/${{ matrix.build_type }}
        run: xvfb-run -a ./QTalk --simple-boot-test

      - name: Run unit tests
        if: matrix.build_type == 'Debug'
        working-directory: ${{ runner.temp }}/build/${{ matrix.build_type }}
        run: xvfb-run -a ./QTalk --unittest

      - name: Package release artifact
        if: matrix.build_type == 'Release'
        working-directory: ${{ runner.temp }}/build/${{ matrix.build_type }}
        run: |
          ls -R
          mkdir -p dist
          cp ./QTalk dist/
          tar czf QTalk-${{ matrix.arch }}-${{ matrix.build_type }}.tar.gz -C dist .

      - name: Create github release
        if: startsWith(github.ref, 'refs/tags/') && matrix.build_type == 'Release'
        uses: softprops/action-gh-release@v2
        with:
            files: ${{ runner.temp }}/build/${{ matrix.build_type }}/QTalk-${{ matrix.arch }}-${{ matrix.build_type }}.tar.gz
            name: QTalk ${{ github.ref_name }}
            body: |
              Auto-generated release for ${{ github.ref_name }}.
              - Build type: ${{ matrix.build_type }}
              - Arch: ${{ matrix.arch }}
        env:
            GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
